# Configuración de Producción - Sistema PQRS
spring.application.name=pqrs-backend

# Configuración de la base de datos - PRODUCCIÓN
spring.datasource.url=jdbc:sqlserver://vmmstsql01:1433;databaseName=mstsupport;encrypt=false;trustServerCertificate=true
spring.datasource.username=localjperez
spring.datasource.password=Jp3r3zc4l*+
spring.datasource.driver-class-name=com.microsoft.sqlserver.jdbc.SQLServerDriver

# Configuración JPA/Hibernate - PRODUCCIÓN
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.SQLServer2012Dialect

# Configuración de Pool de Conexiones - OPTIMIZADO PARA PRODUCCIÓN
spring.datasource.hikari.maximum-pool-size=20
spring.datasource.hikari.minimum-idle=5
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.idle-timeout=600000
spring.datasource.hikari.max-lifetime=1800000
spring.datasource.hikari.leak-detection-threshold=60000

# Configuración JWT - PRODUCCIÓN
jwt.secret=mySecretKeyForProductionEnvironment123456789
jwt.expiration=86400000
jwt.prefix=Bearer 
jwt.header=Authorization

# Configuración de Logging - PRODUCCIÓN
logging.level.root=WARN
logging.level.com.claude.springboot.app=INFO
logging.level.org.springframework.security=WARN
logging.level.org.hibernate=WARN
logging.level.org.springframework.web=WARN

# Configuración de archivos - PRODUCCIÓN
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB
spring.servlet.multipart.enabled=true

# Directorio de uploads - PRODUCCIÓN
file.upload-dir=/opt/pqrs/uploads

# Configuración de correo - PRODUCCIÓN
spring.mail.host=smtp.mintrabajo.gov.co
spring.mail.port=587
spring.mail.username=pqrs@mintrabajo.gov.co
spring.mail.password=password_correo_prod
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.ssl.trust=smtp.mintrabajo.gov.co

# Configuración LDAP/Active Directory - PRODUCCIÓN
ad.url=ldap://mintrabajo.loc:389
ad.domain=mintrabajo.loc
ad.searchBase=DC=mintrabajo,DC=loc
ad.security.authentication=simple

# Service Account para LDAP
ad.serviceAccount.username=service_ldap_user
ad.serviceAccount.password=service_ldap_password

# Configuración de timeouts y reintentos
ad.timeout=5000
ad.retry.count=3
ad.referral=follow

# SSL/TLS para LDAP (si es necesario)
ad.ssl.enabled=false
ad.ssl.trustStore=
ad.ssl.trustStorePassword=

# Frontend URL para producción
app.frontend-url=https://mst.mintrabajo.gov.co

# CORS para producción
cors.allowed-origins=https://mst.mintrabajo.gov.co

# Configuración Actuator para producción
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=when-authorized
management.endpoint.health.show-components=when-authorized
management.info.env.enabled=true
management.info.java.enabled=true
management.info.os.enabled=false

# Información de la aplicación para Actuator
info.app.name=Sistema de Soporte Técnico (PQRS)
info.app.description=API REST para gestión de Peticiones, Quejas, Reclamos y Sugerencias
info.app.version=1.0.0
info.app.encoding=@project.build.sourceEncoding@
info.app.java.version=@java.version@

# Configuración de servidor para producción
server.port=${SERVER_PORT:8080}
server.servlet.context-path=${CONTEXT_PATH:/pqrs-backend}

# Configuración de seguridad adicional
server.error.include-message=never
server.error.include-binding-errors=never
server.error.include-stacktrace=never
server.error.include-exception=false
